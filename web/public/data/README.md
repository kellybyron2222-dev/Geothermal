# Static data contract (Milestone 3)

Place precomputed outputs here after scoring:

- `prospects.json` — ranked screening counties + factor payloads
- `prospects.geojson` — county polygons + score properties
- `meta.json` — methodology_version, weights, vintages

Do not commit restricted raw extracts. See repo `data/raw/` (gitignored).
